<div class="flip-info-content justify-content-center">
  <div class="flip-info-card">
    <div class="flip-front flex-wrap">
      <%= cl_image_tag(user.avatar, :width => 120, :height => 100, :crop => :thumb, :gravity => :face, class: "avatar-bordered") %>
        <p>
          <span class="fa fa-star checked"></span>
          <span class="fa fa-star checked"></span>
          <span class="fa fa-star checked"></span>
          <span class="fa fa-star"></span>
          <%= user[:rating] %></p>
          <div class="user-info">
            <p><strong><%= user[:name] %></strong></p>
            <p><%= user.slots.first.price %>€/15min</p>
        <% wanted = user.slots.find { |s| s.start > DateTime.now} %>
          </div>
       <!--  <p><strong>Topic interests</strong></p> -->
            <% user.tags.each do |t| %>
              <div class ="tags">
                <%= t.name %>
              </div>
                <% end %>
              <div class="user-info-tags">
                <p class="p-4 m-4">Next availablility:<strong> <%= wanted.start_format %></strong></p>
                </div>
            </div>
              <div class="flip-back justify-content-center p-4">
                <p><%= user[:bio] %></p>

      </div>
    </div>
  </div>


