<%= simple_form_for([ user, slot ], remote: true) do |f| %>
  <%= f.input :start, input_html: {placeholder: "#{DateTime.now}"} %>
  <%= f.input :price, input_html: {placeholder: "$"} %>
  <%= f.submit "Add this slot to your profile", class: "btn btn-talk" %>
<% end %>
