<div class="rating-cards">
  <p><%= review.comment %></p>
  <div class="d-flex justify-content-center">
    <% review.rating.times do %>
    <i class="fa fa-star"></i>
    <% end %>
  </div>
  <p> - <em><%= User.find(review.reviewer_id).name %></em></p>
</div>
